# ZipDeez
A private Telegram bot that collects forwarded audio files and documents, then packages them into a single ZIP archive and sends it back — preserving original quality, filenames, and metadata without any re-encoding.
